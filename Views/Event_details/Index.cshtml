@model IEnumerable<Party_Management.Models.event_details>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.party_type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.party_time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Number_of_attendees)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.party_type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.party_time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Number_of_attendees)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
